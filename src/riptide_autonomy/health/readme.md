# Autonomy Health System

System for detecting potential runtime issues in trees before the trees are run. The system provides a suite of detectors that analyze different aspects of the trees and return a list of issues which can be displayed to the user. Some issues are "fixable" and can suggest and if desired, automatically apply fixes to the XML which will resolve the issue.

## Targeted Tree Issues
- [ ] 
- [ ] 
